# 変更履歴

ダウンロード: https://loopbackmix.ic2.jp/

## 1.1（2026-09-21）

#### 変更点
- 設定の「情報」タブから「情報をコピー」ボタンを外しました
- これまでの 1.0.x での改良（ミキサーの表示サイズの変更、交点の L・R ボタンの拡大、設定画面、メニューバーの右クリックメニューなど）をまとめ、バージョンを 1.1 にしました

## 1.0.10（2026-09-21）

#### 変更点
- ミキサーの表示サイズを設定から選べるようにしました（小・標準・大・特大・最大。文字、交点のボタン、メーターがまとめて拡大縮小します）
- 音源とラインの交点の L・R のボタンを、マスいっぱいの長方形にして押しやすくし、文字も大きくしました
- ミキサー画面の左下にバージョンを表示するようにしました
- メニューバーのアイコンを右クリックしたとき、メニューの先頭（設定…）が隠れてしまうのを直しました
- 設定の「情報」タブの設定の保存先を、リンクではなく文字だけの表示にしました

## 1.0.9（2026-09-20）

#### 変更点
- メニューバーのアイコンを右クリック（Control + クリック）すると、設定・アップデートの確認・設定を初期化・終了のメニューが出るようにしました
- メニューバーのパネルから「その他」と「終了」を外し、右クリックのメニューにまとめました
- アプリ内とホームページの「ルーター」という言い方を「ミキサー」に統一しました
- ミキサー画面の説明文（交点のボタンの説明、下の帯の案内）を非表示にしました

## 1.0.8（2026-09-19）

#### 変更点
- 設定画面（⌘,）を追加しました。ラインの管理、取り込み方式、バッファ、自動で追加する項目、除外するアプリはここから設定します
- 音源とモニター先の追加を、画面上部のメニューバーの「音源」「モニター先」に移しました
- 設定画面に「情報」タブを追加し、バージョン、ドライバ、動作環境、著作権などを表示するようにしました
- 画面をスクロールしても、ラインの列見出しと音源の行見出しが隠れないようにしました

## 1.0.7（2026-09-19）

#### 変更点
- 「ライン（モニター元）」の各行に、ライン自体の音量のスライダーとミュートボタンを追加しました（音量キー・コントロールセンターと同じ音量です）
- 音量の数値（dB・%）をクリックすると、値を直接入力できるようにしました

## 1.0.6（2026-09-19）

#### 追加
- アプリの中でアップデートできるようにしました。「今すぐアップデート」を押すと、ダウンロードからインストールまで行い、自動で起動し直します（管理者のパスワードを求められます）。インストーラは、Apple の公証と開発元の署名を確認してから実行します。

※ アプリの中でのアップデートは、1.0.6 から次のバージョンへの更新で使えます。

## 1.0.5（2026-09-19）

#### 変更
- Safari の音を、画面収録を使わずに取り込むようにしました。Safari のウインドウのボタンが「収録中」の表示に変わらなくなり、Safari のために「画面と音声の収録」を許可する必要もなくなりました。

## 1.0.4（2026-09-19）

#### 修正
- ラインのチャンネル数を変えたとき、アプリの画面と接続にすぐ反映されるようにしました（これまでは再起動が必要でした）。

## 1.0.3（2026-09-19）

このバージョンでは仮想オーディオデバイスのドライバも更新されます。インストールの最後にオーディオシステムを再起動するため、再生中の音が一瞬途切れます。

#### 修正
- ラインの名前を変えたとき、アプリとシステム設定の「サウンド」にすぐ反映されるようにしました（これまでは再起動が必要でした）。
- 音量キーでラインの音量を下げると、OBS などが受け取る音まで小さくなっていた問題を修正しました。音量キーは、ラインをモニターしているヘッドフォンなどの音量（聞こえる音量）だけを変えます。
- ラインとヘッドフォンの音量の連動で、音量キーを続けて押すと音量が行ったり来たりしていた問題を修正しました。
- ラインのチャンネルのミュートや音量が、ラインへ直接出力しているアプリの音（システムの出力にラインを選んだとき）にも効くようにしました。

#### 変更
- ラインのモニターとメーターが、実際にラインに流れている音（OBS などが受け取る音）を表示・再生するようにしました。

## 1.0.2（2026-09-19）

#### 修正
- イヤホンやオーディオ機器を抜き差ししたときに、アプリが終了してしまう問題を修正しました。

## 1.0.1（2026-09-19）

#### 追加
- 設定を初期化する機能を追加しました（アプリのメニュー、またはメニューバーの「その他」→「設定を初期化…」）。元の設定はバックアップとして残ります。

#### 変更
- 「アップデートを確認…」をメニューバーの「その他」に移しました。

## 1.0.0（2026-09-19）

最初の公開バージョンです。

#### 主な機能
- 仮想オーディオデバイス「ライン」（最大 16 本、各 2〜64 チャンネル。名前とチャンネル数はアプリの「ラインを管理」から変更）
- アプリごとの音の取り込み、システム全体の音、マイクなどの入力
- 行列型のルーター（交点の LL・LR・RL・RR ボタンで左右の割り当て、ペアを L/R の 2 列に分割）
- 音源・チャンネル・ペア・ライン全体の音量とミュート、列見出しの縦フェーダー
- ヘッドフォンやスピーカーでの同時モニター、ラインとモニター先の音量の連動
- 起動しているアプリ・音を出したアプリを自動で一覧に追加（除外リストあり）
- メニューバーに常駐、ログイン時に起動
- 新しいバージョンの自動確認
- 低遅延（「低遅延」設定で往復 約 6 ms）
